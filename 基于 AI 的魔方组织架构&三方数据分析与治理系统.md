# 基于 AI 的魔方组织架构&三方数据分析与治理系统

## 一、项目概述

### 1.1 项目背景

希沃魔方作为校园数字基座，管理着学校完整的组织架构数据，核心实体包括：

| 实体 | 说明 | 典型属性 |
| --- | --- | --- |
| 教师 | 学校教职工 | 姓名、工号、手机号、邮箱、任教学科、所属部门 |
| 部门 | 学校组织单元 | 部门名称、部门编码、上级部门、负责人 |
| 班级 | 教学班级 | 校区、学段、年级、班级名称 |
| 学生 | 在校学生 | 姓名、学号、性别、所在班级、家长信息 |
| …… | 其他组织架构实体 | 视实际业务数据而定 |

在实际运营中，这些数据需要与第三方系统（企业微信、钉钉、其他教务系统等）保持同步一致。但由于各系统独立维护、同步链路不完善，以下典型问题频繁发生：

- 三方系统存在某学生，希沃魔方缺失该学生记录。
- 希沃魔方中教师归属部门与三方系统不一致。
- 班级学生名单在两系统中存在差异（多、少、错）。
- 部门层级结构对不齐。

### 1.2 项目定位

完成一个端到端的 AI 工程系统，体现 AI 工程师的综合能力：问题定义 -> 方案设计 -> 模型选型 -> 工程落地 -> 效果评估。

## 二、任务目标

### 2.1 总体目标

构建一套基于 AI 的魔方组织架构&三方数据分析与治理系统，能够：

1. **自动发现差异**：对比希沃魔方与三方系统的组织架构数据，识别不一致项。
2. **智能分类归因**：利用 AI 对差异进行分类（缺失、冗余、冲突）并推断原因。
3. **生成治理建议**：输出可执行的数据同步或修正方案。
4. **支持治理执行**：通过 API 或批量操作，将修正方案落地到希沃魔方。

### 2.2 考核维度

| 维度 | 权重 | 说明 |
| --- | ---: | --- |
| 方案设计能力 | 15% | 系统架构是否合理、可扩展 |
| AI 应用能力 | 30% | 是否正确、有效地使用了 AI 技术 |
| 工程实现能力 | 25% | 代码质量、系统可用性、鲁棒性 |
| 数据处理能力 | 15% | 数据清洗、标准化、匹配策略 |
| 文档与表达 | 15% | 文档完整度、逻辑清晰度、答辩表现 |

## 三、技术方案要求

### 3.1 系统架构（参考）

```text
┌───────────────────────────────────────────────────────────────┐
│                         用户界面 / 命令行                     │
├───────────────────────────────────────────────────────────────┤
│                       任务调度 & 流程引擎                      │
├───────────────────┬───────────────────┬───────────────────────┤
│    数据接入层     │    AI 分析引擎    │      治理执行层       │
│                   │                   │                       │
│ · 希沃魔方 API    │ · 实体匹配        │ · 治理方案生成        │
│ · 三方系统 API    │ · 差异检测        │ · 同步 API 调用       │
│ · CSV/Excel 导入  │ · 智能归因        │ · 批量修正            │
│                   │ · 置信度评估      │ · 回滚机制            │
├───────────────────┴───────────────────┴───────────────────────┤
│                       数据存储 & 状态管理                      │
└───────────────────────────────────────────────────────────────┘
```

### 3.2 核心技术要求（参考）

#### 3.2.1 AI 能力应用

##### （1）智能实体匹配（Entity Resolution）

三方系统的实体名称、编码可能与希沃魔方不完全一致。例如：

- 希沃魔方：`高一(1)班`，三方系统：`2024级1班`。
- 希沃魔方：`张三`，三方系统：`张三（语文）`。

要求使用 AI 进行模糊匹配，支持基于语义相似度的实体对齐。可选方案包括：

- 基于 LLM 的语义匹配，如调用大模型做两两比对。
- Embedding + 向量检索，如 `text-embedding + cosine similarity`。
- 混合策略：规则引擎预处理 + AI 兜底。

##### （2）差异智能分类与归因

识别出差异后，自动分类并推断原因：

| 差异类型 | 示例 | 建议操作 |
| --- | --- | --- |
| 希沃缺失 | 三方有学生“李四”，希沃无 | 同步新增到希沃 |
| 希沃冗余 | 希沃有教师“王五”，三方已删除 | 标记确认后删除 |
| 属性冲突 | 张三在两系统中班级不同 | 人工确认后修正 |
| 结构差异 | 部门层级对不齐 | 建议结构调整 |

要求使用 AI 自动对每条差异进行分类和归因，输出结构化的差异报告。

##### （3）治理方案生成

根据差异分析结果，自动生成可执行的治理操作序列，并评估每条操作的风险等级和影响范围。

#### 3.2.3 功能要求

| 条目 | 说明 |
| --- | --- |
| Web 可视化界面 | 提供差异分析的可视化仪表盘 |
| 人机协同机制 | 高风险操作需人工确认，低风险操作自动执行 |
| 增量处理 | 支持定期增量处理，而非每次都全量比对 |
| 性能优化 | 处理万级实体时性能可接受，如使用批量 Embedding、缓存策略 |
| 测试用例 | 编写单元测试和模拟数据生成脚本 |

## 实习生须知

1. **主动沟通**：遇到阻塞及时与导师同步，不要自己闷头死磕。
2. **先设计后编码**：方案评审通过后再进入开发，避免返工。
3. **拥抱 AI 工具**：鼓励使用 Copilot、ChatGPT 等工具提升效率，但需理解每一行代码。
4. **数据安全**：涉及真实数据时必须脱敏，严禁将数据上传到外部服务。
5. **结果导向**：最终交付的是一个可运行的系统，而非一段实验脚本。

## 文档依据与说明

- 本文依据仓库根目录的 5 张项目截图整理，原始截图保持不变。
- 截图目录中未出现 `3.2.2`，本文保留原编号，未擅自补写该节。
- 截图未明确指定技术栈、API 契约、数据字段格式、交付时间及验收流程，这些内容需在方案设计阶段进一步确认。
